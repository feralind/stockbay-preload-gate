// @ts-check
/**
 * Black Market view — retired. Shop, coachmark, and nav entry removed.
 * Save fields / owned relics may still exist for legacy desks.
 */

export function renderBlackMarket(_state) {
  const root = document.getElementById('blackmarket-root');
  if (!root) return;
  root.innerHTML = '';
}
